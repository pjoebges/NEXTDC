<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dhbw.wetterSpiel" >

    <!-- Nennung der benötigten Permissions für den Netzwerkzugriff. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    android:versionCode="1"
    android:versionName="1.0" >
    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.dhbw.wetterSpiel.MainActivity"
            android:configChanges="locale"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Einstellungsactivity -->
        <activity
            android:name="com.dhbw.wetterSpiel.EinstellungenActivity"
            android:configChanges="locale"
            android:screenOrientation="portrait" >
        </activity>
        <!-- Anleitungsactivity -->
        <activity
            android:name="com.dhbw.wetterSpiel.AnleitungActivity"
            android:configChanges="locale"
            android:screenOrientation="portrait" >
        </activity>
        <!-- Levelactivity -->
        <activity
            android:name="com.dhbw.wetterSpiel.LevelActivity"
            android:configChanges="locale"
            android:screenOrientation="portrait" >
        </activity>
        <!-- Spielactivity -->
        <activity
            android:name="com.dhbw.wetterSpiel.SpielActivity"
            android:configChanges="locale"
            android:screenOrientation="portrait" >
        </activity>
    </application>

</manifest>